<% provide(:title, 'All users') %>
<h1>All users</h1>
<% if current_user.admin? %>
  <p><%= link_to "Add New User", new_user_path, class: "btn btn-lg btn-primary", role: "button" %></p>
<% end %>
<%= will_paginate %>
<ul class="users">
  <%= render @users %>
</ul>
<%= will_paginate %>
